---
description: Chronon code quality, clippy, rustdoc, and module discipline
alwaysApply: true
---

# Chronon quality gate

**Repo:** chronon (workspace root) · **Public crate:** `chronon` (`prelude` re-exports) · **Features:** none default; opt-in `mem`, `telemetry-console`, `axum`

## Before finishing

1. `cargo fmt --all -- --check`
2. `cargo clippy --workspace --all-targets -- -D warnings`
3. `cargo test --workspace`
4. `cargo machete` — no unused deps
5. `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --exclude uf-chronon --no-deps`
6. `RUSTDOCFLAGS="-D warnings" cargo doc -p uf-chronon --all-features --no-deps`
7. `cargo test --doc` on crates with `# Examples` (core, backend-mem, runtime, scheduler, executor, axum)

## Module discipline

- **SLOC target ≤400, hard stop 450** (code only; rustdoc excluded)
- **CC ≤25** per function; fan-out ≤15; no god files / kitchen sinks
- No wildcard `pub use foo::*`; thin `lib.rs`; one responsibility per module
- Exemplar: `chronon-axum/src/handlers.rs` — small per-route handlers

## Rustdoc

- `//!` landing pages on crate/submodule roots (getting started, guided modes)
- `///` on every public item; `# Examples` on major entry points
- No `#![allow(missing_docs)]` without explicit approval
- Public crate (`uf-chronon`) has no default features — document it with `--all-features` so backend/`axum` links resolve

## Lint policy

- Clippy: pedantic + nursery + restriction (`unwrap_used`, `expect_used`, `dbg_macro`, `print_stdout`); tests exempt unwrap/expect via `clippy.toml`
- No new `#[allow(dead_code)]` or inline clippy allows — fix or use root `Cargo.toml` / `clippy.toml`
- Layering: core → engine → runtime → public crate; testkit/e2e/bench never imported by lower layers

Full checklist: [CONTRIBUTING.md](../CONTRIBUTING.md)
